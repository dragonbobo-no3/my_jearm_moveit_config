controller_manager:
  ros__parameters:
    update_rate: 100

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    jearm_controller:
      type: joint_trajectory_controller/JointTrajectoryController

jearm_controller:
  ros__parameters:
    joints:
      - joint1
      - joint2
      - joint3
      - joint4
      - joint5
      - joint6
      - joint7

    command_interfaces:
      - position

    state_interfaces:
      - position
      - velocity

    allow_partial_joints_goal: false
    open_loop_control: true

    constraints:
      goal_time: 2.0
      stopped_velocity_tolerance: 0.05
      joint1: {trajectory: 0.3, goal: 0.1}
      joint2: {trajectory: 0.3, goal: 0.1}
      joint3: {trajectory: 0.3, goal: 0.1}
      joint4: {trajectory: 0.3, goal: 0.1}
      joint5: {trajectory: 0.3, goal: 0.1}
      joint6: {trajectory: 0.3, goal: 0.1}
      joint7: {trajectory: 0.3, goal: 0.1}
